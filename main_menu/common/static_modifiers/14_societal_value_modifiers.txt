# Cabinet "Change Societal Values" requires a <side>_progress_cabinet_efficiency
# static modifier to exist for each side of a societal value, or it refuses to
# push that side (societal_values.cpp:296/300). turk_vs_tazik is already moved in
# play by change_societal_value effects and the monthly_towards_turk/tazik
# modifiers; these two markers simply re-enable the generic cabinet route as well.
#
# Neutral by default — no free cabinet bonus for choosing to push. If the cabinet
# push feels too slow/fast, set country_cabinet_efficiency to a non-zero value.
turk_progress_cabinet_efficiency = {
	game_data = { category = country }
	country_cabinet_efficiency = 0
}

tazik_progress_cabinet_efficiency = {
	game_data = { category = country }
	country_cabinet_efficiency = 0
}
