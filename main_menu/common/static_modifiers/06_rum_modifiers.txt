# =====================================================================
# SHATTERED RUM — program-step modifiers (Seljuk Inheritance)
#
# Each program step buys a permanent standing offset against the
# disaster's -0.4/mo legitimacy bleed, so the arc reads: declare →
# bleed → build → break even → vindicate. Like the Farr apparatus
# modifiers, these are artifacts of the program — they persist after
# resolution either way (the Karamanids kept their sikka long after
# the claim broke).
# =====================================================================

# Coin struck in your own name at Konya. Whose name is on the dirham
# is the first question every bazaar asks about a would-be sultan.
rum_sikka_struck_mod = {
	monthly_legitimacy = 0.15
	minting_income_factor = 0.15
}

# The Konya–Sivas trunk road working again — caravans moving under
# your protection along the dead sultanate's commercial spine.
rum_trunk_road_mod = {
	monthly_legitimacy = 0.1
	tax_income_efficiency = 0.05
	global_max_rural_control = 0.02
}

# The diwan of Rum restored — the chancery reform carries the real
# machinery; this is the standing proof that the claimant keeps
# registers like a sultan.
rum_diwan_restored_mod = {
	monthly_legitimacy = 0.15
	country_cabinet_efficiency = 0.05
}

# The Old Capital — Konya held. Unlike the other program artifacts
# this one is REVOKED if the city is lost (the disaster's monthly
# watcher): possession is the whole content of the step.
rum_old_capital_mod = {
	monthly_legitimacy = 0.2
	monthly_prestige = 0.05
}

# The relief pulse: each program step buys five years of breathing
# room on top of its permanent offset (add_and_extend — chaining
# steps chains the relief). The temporary refund half of the
# step-reward pattern.
rum_program_relief_mod = {
	monthly_legitimacy = 0.5
}

# Conquest glory / defeat shame — the field half of the audit,
# mirrored from the farr economy (applied by 20_rum_hooks.txt).
rum_conquest_glory = {
	monthly_legitimacy = 0.4
}

rum_defeat_shame = {
	monthly_legitimacy = -1.0
}
