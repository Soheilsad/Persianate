monthly_country_pulse = {
	events = {
		diwan_character_events.1
		diwan_character_events.2
	}
	
	# effect = {
	# 	# Harvest Credit Cost: High interest rates drain the rural economy
	# 	if = {
	# 		limit = { 
	# 			has_reform = government_reform:persianate_bureaucracy_reform 
	# 			modifier:bank_interest > 0
	# 		}
	# 		
	# 		# Base: Peasant Taxable Income (Proxy for rural economy size)
	# 		set_variable = { 
	# 			name = harvest_credit_base 
	# 			value = "estate(estate_type:peasants_estate).estate_taxable_income" 
	# 		}
	# 		
	# 		# Drain = Base * Interest Rate * 0.1
	# 		set_variable = { 
	# 			name = harvest_credit_drain 
	# 			value = var:harvest_credit_base 
	# 		}
	# 		multiply_variable = { 
	# 			name = harvest_credit_drain 
	# 			value = modifier:bank_interest 
	# 		}
	# 		multiply_variable = { 
	# 			name = harvest_credit_drain 
	# 			value = 0.1 
	# 		}
	# 		
	# 		# Apply Expense
	# 		add_treasury = { 
	# 			value = var:harvest_credit_drain 
	# 			multiply = -1 
	# 		}
	# 	}
	# }
}
