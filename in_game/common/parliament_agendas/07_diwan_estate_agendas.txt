pa_diwan_case_embezzlement = {
	type = country
	estate = diwan_estate
	importance = 2.5
	potential = {
		country_has_estate = estate_type:diwan_estate
	}
	on_accept = {
		add_estate_satisfaction = { type = estate_type:diwan_estate value = -0.20 }
		add_estate_satisfaction = { type = estate_type:amirs_estate value = { add = { value = "estate_power(estate_type:amirs_estate)" multiply = 0.2 } } }
		add_estate_satisfaction = { type = estate_type:clergy_estate value = { add = { value = "estate_power(estate_type:clergy_estate)" multiply = 0.2 } } }
		add_estate_satisfaction = { type = estate_type:burghers_estate value = { add = { value = "estate_power(estate_type:burghers_estate)" multiply = 0.2 } } }
		add_estate_satisfaction = { type = estate_type:peasants_estate value = { add = { value = "estate_power(estate_type:peasants_estate)" multiply = 0.2 } } }

		# Recover the embezzled hoard: a large share of the diwan's accumulated
		# coffers, less whatever they have sheltered in waqf. The crown takes all.
		# Inlined seizure (kept inline so the tooltip can preview the amount — a
		# scripted_effect call resolves only after the tooltip has rendered).
		"estate(estate_type:diwan_estate)" = { save_scope_as = seize_target }
		# LOCAL variable: scopes to this effect execution, not the
		# country entity — no residue, no cross-effect collisions.
		# (diwan_waqf_shelter stays a durable country variable: it is
		# set by the waqf-shelter mechanic and must persist.)
		set_local_variable = {
			name = seized_pool
			value = {
				value = scope:seize_target.estate_gold
				multiply = 0.40
			}
		}
		if = {
			limit = { has_variable = diwan_waqf_shelter }
			change_local_variable = {
				name = seized_pool
				multiply = {
					value = 1
					subtract = var:diwan_waqf_shelter
				}
			}
		}
		add_gold_to_estate = {
			estate_type = estate_type:diwan_estate
			value = {
				value = local_var:seized_pool
				multiply = -1
			}
		}
		add_gold = { value = local_var:seized_pool }

		add_country_modifier = { modifier = diwan_purge_disruption years = 3 }
		add_country_modifier = { modifier = diwan_reform_tax_efficiency years = 10 }
	}
	can_bribe = { can_bribe_default_trigger = yes }
	on_bribe = { on_bribe_default_effect = yes }
	ai_will_do = { base = 100 }
	chance = 10
}

pa_diwan_case_sale_of_offices = {
	type = country
	estate = diwan_estate
	importance = 1.85
	on_accept = {
		add_estate_satisfaction = { type = estate_type:diwan_estate value = -0.15 }
		add_estate_satisfaction = { type = estate_type:clergy_estate value = { add = { value = "estate_power(estate_type:clergy_estate)" multiply = 0.2 } } }
		add_estate_satisfaction = { type = estate_type:amirs_estate value = { add = { value = "estate_power(estate_type:amirs_estate)" multiply = 0.2 } } }
		add_estate_satisfaction = { type = estate_type:burghers_estate value = { add = { value = "estate_power(estate_type:burghers_estate)" multiply = 0.2 } } }
		add_estate_satisfaction = { type = estate_type:peasants_estate value = { add = { value = "estate_power(estate_type:peasants_estate)" multiply = 0.2 } } }
		add_country_modifier = { modifier = diwan_purge_disruption years = 1 }
		# add_country_modifier = { modifier = diwan_reform_admin_efficiency years = -1 }
	}
	can_bribe = { can_bribe_default_trigger = yes }
	on_bribe = { on_bribe_default_effect = yes }
	ai_will_do = { base = 100 }
	chance = 10
}

pa_diwan_case_falsified_surveys = {
	type = country
	estate = diwan_estate
	importance = 2.5
	on_accept = {
		add_estate_satisfaction = { type = estate_type:diwan_estate value = -0.18 }
		add_estate_satisfaction = { type = estate_type:peasants_estate value = { add = { value = "estate_power(estate_type:peasants_estate)" multiply = 0.2 } add = 0.10 } }
		add_estate_satisfaction = { type = estate_type:amirs_estate value = { add = { value = "estate_power(estate_type:amirs_estate)" multiply = 0.2 } } }
		add_estate_satisfaction = { type = estate_type:clergy_estate value = { add = { value = "estate_power(estate_type:clergy_estate)" multiply = 0.2 } } }
		add_estate_satisfaction = { type = estate_type:burghers_estate value = { add = { value = "estate_power(estate_type:burghers_estate)" multiply = 0.2 } } }

		# Refund (State Pays)
		add_gold = { value = root.monthly_income_trade_and_tax multiply = -12 }

		add_stability = 1
		random_owned_location = {
			limit = { is_capital = no }
			add_location_modifier = { modifier = diwan_local_goods_boost years = 10 }
		}
	}
	can_bribe = { can_bribe_default_trigger = yes }
	on_bribe = { on_bribe_default_effect = yes }
	ai_will_do = { base = 100 }
	chance = 10
}

pa_diwan_case_inflated_costs = {
	type = country
	estate = diwan_estate
	importance = 1.85
	on_accept = {
		add_estate_satisfaction = { type = estate_type:diwan_estate value = -0.12 }
		add_estate_satisfaction = { type = estate_type:amirs_estate value = { add = { value = "estate_power(estate_type:amirs_estate)" multiply = 0.2 } } }
		add_estate_satisfaction = { type = estate_type:clergy_estate value = { add = { value = "estate_power(estate_type:clergy_estate)" multiply = 0.2 } } }
		add_estate_satisfaction = { type = estate_type:burghers_estate value = { add = { value = "estate_power(estate_type:burghers_estate)" multiply = 0.2 } } }
		add_estate_satisfaction = { type = estate_type:peasants_estate value = { add = { value = "estate_power(estate_type:peasants_estate)" multiply = 0.2 } } }

		# Claw back the padded accounts: a moderate share of the diwan's hoard,
		# less the waqf shelter. The crown takes all.
		# Inlined seizure (kept inline so the tooltip can preview the amount — a
		# scripted_effect call resolves only after the tooltip has rendered).
		"estate(estate_type:diwan_estate)" = { save_scope_as = seize_target }
		set_local_variable = {
			name = seized_pool
			value = {
				value = scope:seize_target.estate_gold
				multiply = 0.25
			}
		}
		if = {
			limit = { has_variable = diwan_waqf_shelter }
			change_local_variable = {
				name = seized_pool
				multiply = {
					value = 1
					subtract = var:diwan_waqf_shelter
				}
			}
		}
		add_gold_to_estate = {
			estate_type = estate_type:diwan_estate
			value = {
				value = local_var:seized_pool
				multiply = -1
			}
		}
		add_gold = { value = local_var:seized_pool }

		add_country_modifier = { modifier = diwan_reform_build_cost years = 5 }
	}
	can_bribe = { can_bribe_default_trigger = yes }
	on_bribe = { on_bribe_default_effect = yes }
	ai_will_do = { base = 100 }
	chance = 10
}

pa_diwan_case_assessment_bribes = {
	type = country
	estate = diwan_estate
	importance = 1.85
	on_accept = {
		add_estate_satisfaction = { type = estate_type:diwan_estate value = -0.15 }
		add_estate_satisfaction = { type = estate_type:burghers_estate value = { add = { value = "estate_power(estate_type:burghers_estate)" multiply = 0.2 } add = -0.05 } }
		add_estate_satisfaction = { type = estate_type:peasants_estate value = { add = { value = "estate_power(estate_type:peasants_estate)" multiply = 0.2 } add = 0.05 } }
		add_estate_satisfaction = { type = estate_type:amirs_estate value = { add = { value = "estate_power(estate_type:amirs_estate)" multiply = 0.2 } } }
		add_estate_satisfaction = { type = estate_type:clergy_estate value = { add = { value = "estate_power(estate_type:clergy_estate)" multiply = 0.2 } } }
		add_country_modifier = { modifier = diwan_reform_tax_efficiency years = 10 }
		add_country_modifier = { modifier = diwan_purge_unrest_reduction years = 10 }
	}
	can_bribe = { can_bribe_default_trigger = yes }
	on_bribe = { on_bribe_default_effect = yes }
	ai_will_do = { base = 100 }
	chance = 10
}

pa_diwan_case_fictional_positions = {
	type = country
	estate = diwan_estate
	importance = 2.5
	on_accept = {
		add_estate_satisfaction = { type = estate_type:diwan_estate value = -0.20 }
		add_estate_satisfaction = { type = estate_type:amirs_estate value = { add = { value = "estate_power(estate_type:amirs_estate)" multiply = 0.2 } } }
		add_estate_satisfaction = { type = estate_type:clergy_estate value = { add = { value = "estate_power(estate_type:clergy_estate)" multiply = 0.2 } } }
		add_estate_satisfaction = { type = estate_type:burghers_estate value = { add = { value = "estate_power(estate_type:burghers_estate)" multiply = 0.2 } } }
		add_estate_satisfaction = { type = estate_type:peasants_estate value = { add = { value = "estate_power(estate_type:peasants_estate)" multiply = 0.2 } } }

		# Strike the phantom salaries from the rolls: a small share of the
		# diwan's hoard, less the waqf shelter. The crown takes all.
		# Inlined seizure (kept inline so the tooltip can preview the amount — a
		# scripted_effect call resolves only after the tooltip has rendered).
		"estate(estate_type:diwan_estate)" = { save_scope_as = seize_target }
		set_local_variable = {
			name = seized_pool
			value = {
				value = scope:seize_target.estate_gold
				multiply = 0.15
			}
		}

		# Guard was missing here (the other two cases have it): with no
		# waqf shelter established, var:diwan_waqf_shelter is undefined
		# and the multiply block misbehaves.
		if = {
			limit = { has_variable = diwan_waqf_shelter }
			change_local_variable = {
				name = seized_pool
				multiply = {
					value = 1
					subtract = var:diwan_waqf_shelter
				}
			}
		}

		add_gold_to_estate = {
			estate_type = estate_type:diwan_estate
			value = {
				value = local_var:seized_pool
				multiply = -1
			}
		}
		add_gold = { value = local_var:seized_pool }

		add_country_modifier = { modifier = diwan_reform_state_maintenance years = 5 }
	}
	can_bribe = { can_bribe_default_trigger = yes }
	on_bribe = { on_bribe_default_effect = yes }
	ai_will_do = { base = 100 }
	chance = 10
}
