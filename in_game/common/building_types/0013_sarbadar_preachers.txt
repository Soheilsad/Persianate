# =====================================================================
# SARBADAR PREACHERS — the covert cell of the Jūrī network
#
# Host-owned, planted ONLY by the sarbadar_phenomenon situation
# (country_potential = always no: nobody builds this on purpose —
# construct_building from script bypasses the gate, mirroring how
# lutheran_preachers appear in Catholic realms). The host can crush
# the cell at a price (gold and a stability knock — the crackdown is
# public and ugly); the cell dissolves on its own if a commonwealth
# takes the city (it becomes the establishment) or the phenomenon
# ends. Modeled on lutheran_preachers (situation_buildings.txt).
# =====================================================================

sarbadar_preachers = {
	is_special = yes
	is_foreign = no
	max_levels = 1
	pop_type = clergy
	employment_size = 0.02

	category = religious_category

	can_close = no

	rural_settlement = no
	town = yes
	city = yes
	megalopolis = yes
	conversion_religion = shia
	
	build_time = generic_event_building

	# Nobody builds the cell by hand — the network plants it.
	country_potential = {
		always = no
	}

	remove_if = {
		OR = {
			owner ?= {
				government_type = government_type:commonwealth_of_martyrs
			}
			AND = {
				situation:sarbadar_phenomenon = {
					situation_is_active = no
					situation_has_ended = yes
				}
			}
			# Save-cleaner: a cell with no Muslim quarter to preach to
			# dissolves. Retroactively removes cells that leaked into
			# Christian Europe before the region gate existed.
			NOT = {
				any_pop = {
					religion.group = religion_group:muslim
				}
			}
		}
	}

	destroy_price = remove_sarbadar_preachers

	modifier = {
		# The quarter listens to the shaykh before the governor.
		local_monthly_control = -0.005
		# And it simmers: the cell recruits for the rising to come.
		local_unrest = 0.5
		local_pop_join_rebel_threshold = -0.05
		building_missionary_effort_scaled = 0.01
	}

	construction_demand = estate_construct_building
}
