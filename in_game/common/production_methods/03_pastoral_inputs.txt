# Pastoral grazing — no wheat input. The steppe horse eats grass,
# not stored fodder; the yaylāq–qishlāq circuit is precisely the
# institution that makes stored feed unnecessary. The cost of this
# building is therefore not in goods but in carrying capacity (the
# pasture displaces ploughland) and in labor (a large tribesmen
# workforce per level).
#
# Shared between tribal_pasture and amirs_herd — the recipe is the
# same; the buildings differ only in scale.

tribal_pasture_grazing = {
	produced = horses
	output  = 1.8
	category = building_maintenance
}
tribal_livestock_production = {
	produced = livestock
	output  = 3
	category = building_maintenance
}
