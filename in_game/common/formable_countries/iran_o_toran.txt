# =====================================================================
# ĪRĀN O TŪRĀN — the Türk victor's summation
#
# Mirror of base ILK_f, refit for the Erānzamīn succession's Türk
# outcome: united_iran_turk_unlocked is the ONE gate (set only by the
# Türk victory resolution in ilkhanate_events — everything else, IO,
# contest, matter, is already settled upstream of that variable).
#
# Tag is deliberately ILK: the thing the Türk victor founds IS the
# ulus reborn — same banner, same map color, the state the Ilkhans
# built — but the name on it is what the pairing always was in the
# Shahnama's geography: Iran and Turan under one crown.
# Footprint = the Erānzamīn world (is_eranzamin_region_province).
# =====================================================================

IOT_f = {
	level = 4
	required_locations_fraction = 0.75
	rule = historical
	potential = {
		has_reform = government_reform:chinggisid_regency
	}
	allow = {
		custom_tooltip = {
			text = iot_f_legitimacy_tt
			has_variable = united_iran_turk_unlocked
		}
	}

	name = IOT_NAME
	flag = ILK
	adjective = IOT_ADJ
	tag = ILK
	color = map_ILK

	regions = {
		persia_region
		khorasan_region
		caucasus_region
	}
	areas = {
		cappadocia_area
		iraq_arabi_area
		jazira_area
	}

	form_effect = {
		if = {
			limit = { country_rank_level < 4 }
			set_country_rank_effect = { rank = country_rank:rank_empire }
		}
	}
}
