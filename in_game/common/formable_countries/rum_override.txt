# =====================================================================
# REPLACE: base RUM_f (00_formable_countries.txt:3487) — the two-door
# gate. Base required only the race victory (winner variable); now the
# crown of Rum needs a LEGITIMACY answer as well:
#   - the Konya door: rum_seljuk_vindicated (Seljuk Inheritance
#     disaster, self-declared — you hold the dar al-mulk and the
#     diwan; Cairo's opinion is noted and filed), or
#   - the Cairo door: rum_caliph_sanction (petition_caliph_for_rum,
#     the Bayezid 1394 precedent — vindicated ghaza + chancery +
#     the embassy's gifts).
# Everything else base verbatim: 85% of Anatolia, tag TUR, empire.
# =====================================================================

REPLACE:RUM_f = {
	level = 4
	required_locations_fraction = 0.85
	rule = historical
	potential = {
		culture = { has_culture_group = culture_group:turkic_group }
		religion.group = religion_group:muslim
	}
	allow = {

		custom_tooltip = {
			text = concluded_roto_as_not_ottos_tt
			has_variable = rise_of_the_ottomans_winner_variable
		}
		custom_tooltip = {
			text = rum_two_doors_tt
			OR = {
				has_variable = rum_seljuk_vindicated
				has_variable = rum_caliph_sanction
			}
		}
	}

	name = RUM
	flag = RUM
	adjective = RUM_ADJ
	tag = TUR
	color = map_RUM

	regions = {
		anatolia_region
	}
	form_effect = {
		if = {
			limit = { country_rank_level < 4 }
			set_country_rank_effect = { rank = country_rank:rank_empire }
		}
	}
}
