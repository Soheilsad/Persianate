IRA_f = {
	level = 4
	required_locations_fraction = 0.75
	rule = historical
	potential = {
		culture = { has_culture_group = culture_group:iranian_group }
		owns = location:tabriz
        owns = location:isfahan
        owns = location:shiraz
	}
	# Forming Persia is the constitutional summation, not a map-paint
	# reward: the legitimacy question settled (farr or ghazi wager won
	# or endured — broken outcomes do NOT qualify — or the murid bond
	# sworn: the Safavid case, a settled charismatic legitimacy), the
	# Türk question answered (or never posed), and the crown worn in
	# one's own name (a regent cannot found Erānshahr).
	allow = {
		# 3 — incoherent under the borrowed mandate.
		NOT = { has_reform = government_reform:chinggisid_regency }

		# 2 — by what right do you rule: answered, favorably.
		custom_tooltip = {
			text = ira_f_legitimacy_tt
			OR = {
				has_variable = farr_proof_vindicated
				has_variable = farr_proof_endured
				has_variable = ghazi_sword_of_islam
				has_variable = ghazi_guardian_of_thughur
				# The murid bond: the Safavid road — delete this line
				# if the mystical legitimacy should not qualify.
				has_variable = matter_sufi_synthesis
			}
		}

		# 1 — what is the sword's place: answered (and not by the
		# sword leaving), or never posed (no Türk demographic base —
		# the rural-gentry realm has no Matter to resolve).
		custom_tooltip = {
			text = ira_f_matter_tt
			OR = {
				AND = {
					has_variable = matter_of_tork_resolved
					NOT = { has_country_modifier = matter_turk_defection_scar }
				}
				AND = {
					NOT = { has_variable = matter_of_tork_fired }
					num_pop_type_in_country:tribesmen < 750
				}
			}
		}
	}
	name = IRA
	flag = IRA
	adjective = IRA_ADJ
	tag = IRA
	color = map_IRA
	regions = {
		persia_region
	}
	form_effect = {
		if = {
			limit = { country_rank_level < 4 }
			set_country_rank_effect = { rank = country_rank:rank_empire }
		}
	}
}