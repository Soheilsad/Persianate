namespace = sarbadar_phenomenon

# =====================================================================
# sarbadar_phenomenon.1 — THE WORD FROM BASHTIN (situation start)
#
# Fired from the situation's on_start to every country that can see
# it. Namespace matches the situation key so the event window pulls
# the situation icon (situation_modding.md).
# =====================================================================
sarbadar_phenomenon.1 = {
	type = country_event
	category = situation_event
	outcome = neutral
	title = sarbadar_phenomenon.1.title
	desc = sarbadar_phenomenon.1.desc

	illustration_tags = {
		10 = exterior
		10 = angry
	}

	option = {
		name = sarbadar_phenomenon.1.a
	}
}
